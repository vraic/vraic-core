<%= form_with(model: order_item, class: "contents") do |form| %>
  <% if order_item.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul class="list-disc ml-6">
        <% order_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": order_item.errors[:order_id].none?, "border-red-400 focus:outline-red-600": order_item.errors[:order_id].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :inventory_item_id %>
    <%= form.text_field :inventory_item_id, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": order_item.errors[:inventory_item_id].none?, "border-red-400 focus:outline-red-600": order_item.errors[:inventory_item_id].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": order_item.errors[:quantity].none?, "border-red-400 focus:outline-red-600": order_item.errors[:quantity].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :price_cents %>
    <%= form.number_field :price_cents, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": order_item.errors[:price_cents].none?, "border-red-400 focus:outline-red-600": order_item.errors[:price_cents].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :currency %>
    <%= form.text_field :currency, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": order_item.errors[:currency].none?, "border-red-400 focus:outline-red-600": order_item.errors[:currency].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
